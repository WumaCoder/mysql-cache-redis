{
  "name": "mysql-cache-redis",
  "version": "0.0.4",
  "description": "A nodejs mysql caching framework based on redis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "redis",
    "mysql",
    "cache"
  ],
  "author": "mark",
  "license": "ISC",
  "dependencies": {
    "mysql": "^2.16.0",
    "redis": "^2.8.0"
  }
}
